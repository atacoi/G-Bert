{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch G-Bert",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/${config:executable_name}",
      "preLaunchTask": "OpenGL Build",
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb"
    }
  ]
}
